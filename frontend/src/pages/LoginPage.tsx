import { useState, type FormEvent } from 'react'
import { useAuth } from '../auth'
import { StockFlowLogo } from '../components/AppShell'
export function LoginPage(){const {login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false)
  async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError('');try{await login(email,password)}catch{setError('Invalid email or password')}finally{setPassword('');setBusy(false)}}
  return <main className="auth-page"><form className="auth-card login-card" onSubmit={submit}><div className="login-brand"><StockFlowLogo/><span><strong>StockFlow</strong><small>Inventory management</small></span></div><div><span className="eyebrow">Secure workspace</span><h1>Welcome back</h1><p>Sign in to continue to StockFlow.</p></div><label>Email<input type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button primary" disabled={busy}>{busy?'Signing in…':'Sign In'}</button></form></main>}
