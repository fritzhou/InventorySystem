import { useEffect,useState } from 'react';import { api } from '../services/api';import type { AuthUser,Role } from '../auth'
export function UsersPage(){const [users,setUsers]=useState<AuthUser[]>([]);const [error,setError]=useState('');const load=()=>api.getUsers().then(setUsers).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function add(){const email=prompt('Email');const name=prompt('Display name');const role=prompt('Role: ADMIN, MANAGER, or CASHIER','CASHIER') as Role|null;const password=prompt('Temporary password (at least 10 characters)');if(email&&name&&role&&password){await api.createUser({email,display_name:name,role,temporary_password:password});load()}}async function deactivate(user:AuthUser){if(confirm(`Deactivate ${user.display_name}?`)){await api.updateUser(user.id,{is_active:false});load()}}async function reset(user:AuthUser){const password=prompt('New temporary password (at least 10 characters)');if(password){await api.resetUserPassword(user.id,password);alert('Password reset. The user must change it at next sign in.')}}return <section><div className="page-heading"><div><span className="eyebrow">Administration</span><h1>Users</h1><p>Manage roles, account status, and secure access to StockFlow.</p></div><button className="button primary" onClick={add}>+ Add User</button></div>{error&&<p className="error" role="alert">{error}</p>}<div className="card table-wrap user-table"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Last Login</th><th>Actions</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.display_name}</td><td>{u.email}</td><td><span className="role-badge">{u.role}</span></td><td><span className={`status-pill ${u.is_active?'active':'inactive'}`}>{u.is_active?'Active':'Inactive'}</span></td><td>{u.last_login_at?new Date(u.last_login_at).toLocaleString():'Never'}</td><td><button className="text-button danger" disabled={!u.is_active} onClick={()=>deactivate(u)}>Deactivate</button> <button className="text-button" onClick={()=>reset(u)}>Reset password</button></td></tr>)}</tbody></table></div></section>}
