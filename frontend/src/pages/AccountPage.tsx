import { useState, type FormEvent } from 'react';import { api } from '../services/api';import { useAuth } from '../auth'
export function AccountPage(){const {user,refresh}=useAuth();const [current,setCurrent]=useState('');const [next,setNext]=useState('');const [message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await api.changePassword(current,next);setMessage('Password changed.');setCurrent('');setNext('');await refresh()}catch(e){setMessage(e instanceof Error?e.message:'Password could not be changed')}}return <section><h1>Account</h1><p><strong>{user?.display_name}</strong> · {user?.role}</p><form className="panel auth-card" onSubmit={submit}><h2>Change Password</h2><label>Current password<input type="password" value={current} onChange={e=>setCurrent(e.target.value)} required/></label><label>New password<input type="password" minLength={10} value={next} onChange={e=>setNext(e.target.value)} required/></label><button>Change Password</button>{message&&<p role="status">{message}</p>}</form></section>}
