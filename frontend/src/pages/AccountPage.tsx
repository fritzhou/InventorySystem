import { useState, type FormEvent } from 'react'
import { api } from '../services/api'
import { useAuth } from '../auth'
export function AccountPage(){const {user,refresh}=useAuth();const [current,setCurrent]=useState('');const [next,setNext]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');const [saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError('');setMessage('');try{await api.changePassword(current,next);setMessage('Your password was changed successfully.');setCurrent('');setNext('');await refresh()}catch(e){setError(e instanceof Error?e.message:'Password could not be changed')}finally{setSaving(false)}}return <section><div className="page-heading"><div><span className="eyebrow">Profile & security</span><h1>Account</h1><p>Review your StockFlow identity and keep your account secure.</p></div></div><div className="account-grid"><section className="card profile-card"><span className="profile-avatar">{user?.display_name.slice(0,2).toUpperCase()}</span><div><h2>{user?.display_name}</h2><p>{user?.email}</p><span className="status-pill active">{user?.role}</span></div></section><form className="card security-card" onSubmit={submit}><span className="eyebrow">Security</span><h2>Change password</h2><p>Use at least 10 characters. You will use the new password the next time you sign in.</p><label>Current password<input autoComplete="current-password" type="password" value={current} onChange={e=>setCurrent(e.target.value)} required/></label><label>New password<input autoComplete="new-password" type="password" minLength={10} value={next} onChange={e=>setNext(e.target.value)} required/></label>{error&&<p className="error" role="alert">{error}</p>}{message&&<p className="success-note" role="status">{message}</p>}<div className="form-actions"><button className="button primary" disabled={saving}>{saving?'Changing…':'Change password'}</button></div></form></div></section>}
