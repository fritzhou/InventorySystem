import { useEffect,useState } from 'react';import { api,type AuditEvent } from '../services/api'
export function AuditLogPage(){const [items,setItems]=useState<AuditEvent[]>([]);const [action,setAction]=useState('');const [page,setPage]=useState(1);useEffect(()=>{api.getAuditEvents(action,page).then(r=>setItems(r.items))},[action,page]);return <section><h1>Audit Log</h1><div className="filters"><label>Action<input value={action} onChange={e=>{setAction(e.target.value);setPage(1)}}/></label></div><div className="table-wrap"><table><thead><tr><th>Date</th><th>Actor</th><th>Action</th><th>Entity</th></tr></thead><tbody>{items.map(e=><tr key={e.id}><td>{new Date(e.created_at).toLocaleString()}</td><td>{e.actor_display_name??'System'}</td><td>{e.action}</td><td>{e.entity_id??e.entity_type}</td></tr>)}</tbody></table></div><button disabled={page===1} onClick={()=>setPage(p=>p-1)}>Previous</button> <button onClick={()=>setPage(p=>p+1)}>Next</button></section>}
